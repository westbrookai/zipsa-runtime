# Environment Variables Template
# Copy this file to env.txt and fill in your actual values
# IMPORTANT: Never commit env.txt to git (it's in .gitignore)

# Anthropic API Key (required for Claude Code)
export ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Google OAuth (optional, for MCP servers requiring OAuth)
export GOOGLE_OAUTH_CLIENT_ID="your-google-oauth-client-id"
export GOOGLE_OAUTH_CLIENT_SECRET="your-google-oauth-client-secret"

# MCP OAuth Configuration (optional)
export MCP_ENABLE_OAUTH21=false
export EXTERNAL_OAUTH21_PROVIDER=true

# Usage:
# 1. Copy this file: cp env.example.txt env.txt
# 2. Edit env.txt with your actual credentials
# 3. Load variables: source env.txt
# 4. Run Docker with env vars:
#    docker run -e ANTHROPIC_API_KEY -v $(pwd):/workspace skill-runtime:latest claude
